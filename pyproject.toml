[project]
name = "construction-chatbot"
version = "0.1.0"
description = "A RAG-based chatbot for construction documents"
license = { text = "MIT" }
readme = "README.md"
requires-python = ">=3.9"

dependencies = [
  "fastapi",
  "uvicorn",
  "chromadb",
  "openai",
  "redis",             # ← corrected
  "pydantic-settings",
  "python-dotenv",
  "PyMuPDF",
  "typer"
]
